#include <stdio.h>
int main()
{
    int m=15, n=25, max;
    max = m>n? m: n;
    while(1) 
    {
        if (max%m == 0 && max % n == 0) 
            break;
        ++max;
    }
    printf("The LCM is %d\n",max);
    return 0;
}