#include <stdio.h>
#include <math.h>
int main()
{
    int n=1024;
    printf("The ln is: %lf\n",log(n));
    printf("The log2 is: %lf\n",log2(n));
    printf("The log10 is: %lf\n",log10(n));
    return 0;
}